describe("wordCounter", () => {
    "use strict";

    let fixture;

    beforeEach(() => {
        fixture = {};
    });

    it("should count words", () => {
       expect(fixture).not.toBeNull();
   });

});